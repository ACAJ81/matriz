<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finance | Análise Técnica</title>
    
    <style>
        /* CONFIGURAÇÕES GERAIS */
        :root {
            --primary: #000000; /* Preto para autoridade */
            --accent: #2ecc71;  /* Verde para representar ganhos/candlesticks positivos */
            --bg: #ffffff;
            --text-main: #1a1a1a;
            --text-muted: #666666;
            --border: #eeeeee;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            line-height: 1.6;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* CABEÇALHO */
        header {
            padding: 25px 0;
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            background: white;
            z-index: 100;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            letter-spacing: -1.5px;
            text-transform: uppercase;
            color: var(--primary);
        }

        nav ul {
            display: flex;
            list-style: none;
        }

        nav ul li {
            margin-left: 25px;
        }

        nav ul li a {
            text-decoration: none;
            color: var(--text-main);
            font-weight: 500;
            font-size: 0.9rem;
            transition: color 0.2s;
        }

        nav ul li a:hover {
            color: var(--accent);
        }

        /* ÁREA DE DESTAQUE (HERO) */
        .hero {
            padding: 80px 0 40px;
            text-align: center;
        }

        .hero h1 {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 15px;
            letter-spacing: -1px;
        }

        .hero p {
            color: var(--text-muted);
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto;
        }

        /* ÁREA DO GRÁFICO */
        .chart-box {
            margin: 40px 0;
            background: #fafafa;
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
        }

        .chart-header {
            padding: 15px 20px;
            border-bottom: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: white;
        }

        .badge-live {
            background: #ffebeb;
            color: #e74c3c;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .chart-display {
            height: 600px; /* Aumentado para melhor visualização do gráfico */
            width: 100%;
            background: #fdfdfd;
        }

        /* GRADES DE CONTEÚDO */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-bottom: 80px;
        }

        .card {
            padding: 30px;
            border: 1px solid var(--border);
            border-radius: 12px;
            transition: all 0.3s ease;
        }

        .card:hover {
            border-color: var(--primary);
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }

        .card h3 {
            margin-bottom: 15px;
            font-size: 1.2rem;
        }

        .placeholder-text {
            color: var(--text-muted);
            font-size: 0.9rem;
            line-height: 1.5;
        }

        /* RODAPÉ */
        footer {
            padding: 50px 0;
            border-top: 1px solid var(--border);
            text-align: center;
            color: var(--text-muted);
            font-size: 0.85rem;
        }

        /* RESPONSIVIDADE */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.2rem; }
            nav ul { display: none; }
        }
    </style>
</head>
<body>

    <header>
        <div class="container">
            <nav>
                <div class="logo">Finance</div>
                <ul>
                    <li><a href="#">Mercados</a></li>
                    <li><a href="#">Estratégias</a></li>
                    <li><a href="#">Setup</a></li>
                    <li><a href="#">Sobre</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="container">
        <section class="hero">
            <h1>Análise Técnica Simplificada</h1>
            <p>Acompanhe a evolução do mercado e refine suas tomadas de decisão com dados precisos.</p>
        </section>

        <section class="chart-box">
            <div class="chart-header">
                <strong>Monitoramento Ativo: Mini Índice (WIN)</strong>
                <div class="badge-live">● AO VIVO</div>
            </div>
            <div class="chart-display">
                <div id="tradingview_win" style="width: 100%; height: 100%;"></div>
                <script type="text/javascript" src="https://s3.tradingview.com/tv.js"></script>
                <script type="text/javascript">
                new TradingView.widget({
                    "autosize": true,
                    "symbol": "BMFBOVESPA:WIN1!",
                    "interval": "5",
                    "timezone": "America/Sao_Paulo",
                    "theme": "light",
                    "style": "1",
                    "locale": "br",
                    "toolbar_bg": "#f1f3f6",
                    "enable_publishing": false,
                    "hide_top_toolbar": false,
                    "save_image": false,
                    "container_id": "tradingview_win"
                });
                </script>
                </div>
        </section>

        <section class="grid">
            <div class="card">
                <h3>Últimas Análises</h3>
                <p class="placeholder-text">Aguardando as primeiras movimentações do mercado para publicar as projeções iniciais.</p>
            </div>
            <div class="card">
                <h3>Educação Financeira</h3>
                <p class="placeholder-text">Espaço reservado para conceitos de Price Action, suportes, resistências e indicadores.</p>
            </div>
            <div class="card">
                <h3>Minha Carteira</h3>
                <p class="placeholder-text">Estrutura preparada para exibir o desempenho e a evolução dos ativos monitorados.</p>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2026 FINANCE. O mercado recompensa a disciplina.</p>
        </div>
    </footer>

</body>
</html>
